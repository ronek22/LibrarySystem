<p id="notice" class="text-center"><%= notice %></p>
<div class="container">
  <div class="d-inline-block">
    <h1>Editing Genre: <%= @genre.name %></h1>
  </div>

  <%= render 'form', genre: @genre %>
  <hr/>
  <%= link_to 'Details', @genre, :class => "btn btn-info btn-sm" %> |
  <%= link_to 'Back', genres_path, :class => "btn btn-info btn-sm" %>
</div>
