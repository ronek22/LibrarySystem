<p id="notice" class="text-center"><%= notice %></p>
  <h1>Genre details</h1>
  <p>
    <strong>Name:</strong>
    <%= @genre.name %>
  </p>

  <p>
    <strong>Description:</strong>
    <%= @genre.desc %>
  </p>

<%= link_to 'Back', genres_path, class: 'btn btn-outline-info btn-sm' %>
<%= link_to 'Edit', edit_genre_path(@genre), class: 'btn btn-info btn-sm' %>
<%= link_to 'Destroy', @genre, method: :delete, data: { confirm: 'Are you sure?' }, class: 'btn btn-danger btn-sm' %>
